// Static MV3 MAIN entry, document_start, top-frame https://app.slack.com/client*.
// The isolated content.js is a relay only; never inject this entry dynamically.
import './bridge-setup.ts';
import '../app/main.ts';
